# v2.2.0 validator correction

The first v2.2.0 package contained the updated application source but retained four validation assertions from v2.1.3. That caused GitHub Actions to stop before compilation despite the intended changes being present.

Corrected assertions:

- v2.1.3 / 213 -> v2.2.0 / 220
- TAZALARM label -> TAZLARM label
- old full-logo implementation -> selected cat mark plus TAZLARM wordmark implementation
- singular saved-code helper -> multi-barcode saved-codes helper

The workflow artifact is now named `TAZLARM-v2.2.0-installable-APK`.
