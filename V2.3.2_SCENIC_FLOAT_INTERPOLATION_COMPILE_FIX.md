# TAZLARM v2.3.2 scenic renderer compile fix

GitHub compilation exposed two calls where the imported Compose `lerp` function was used with `Float` values. That import only accepts `Color` or `Shadow` in this source file. The sun centre and sun radius now use explicit float interpolation, preserving the same animation while compiling correctly.
